@model IEnumerable<DataAccessLibrary.Models.Account>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/css/users_show.css" asp-append-version="true" />
</head>
<body>
    <div class="container">
        <div class="main-data">
            <h1>Пользователи</h1>
            <hr />
            <table border="0" class="user_table">
                <tr>
                    <th class="first-column">Табельный номер</th>
                    <th class="second-column">ФИО</th>
                    <th class="third-column">Телефон</th>
                    <th class="fourth-column">Email</th>
                    <th class="fifth-column">Роль</th>
                    <th class="sixth-column">Статус</th>
                </tr>
                @foreach (var account in Model)
                {
                    <tr>
                        <th class="first-column">@account.Profile.ProfileTabNum</th>
                        <th class="second-column">@account.Profile.ProfileSurname @account.Profile.ProfileName @account.Profile.ProfilePatronymic</th>
                        <th class="third-column">@account.Profile.Phones.Where(ph => ph.PhonesIsPriority == true).FirstOrDefault()?.PhonesNum</th>
                        <th class="fourth-column">@account.Profile.Mail.Where(m => m.TypeMail.TypeMailName == "Корпоративная").FirstOrDefault()?.MailsName</th>
                        <th class="fifth-column"></th>
                        <th class="sixth-column"></th>
                    </tr>
                }
            </table>
        </div>
    </div>
</body>
</html>