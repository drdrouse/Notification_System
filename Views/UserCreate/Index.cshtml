@model IEnumerable<DataAccessLibrary.Models.Profile>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="~/css/users_show.css" asp-append-version="true" />
</head>
<body>
    <div class="container">
        <div class="main-data">
            <h1>Пользователи</h1>
            <hr />
            <table border="0" class="user_table">
                <tr>
                    <th class="first-column">Табельный номер</th>
                    <th class="second-column">ФИО</th>
                    <th class="third-column">Телефон</th>
                    <th class="fourth-column">Email</th>
                    <th class="fifth-column">Роль</th>
                    <th class="sixth-column">Статус</th>
                </tr>
                @foreach (var profile in Model)
                {
                    <tr>
                        <th class="first-column">@profile.ProfileTabNum</th>
                        <th class="second-column">@profile.ProfileSurname @profile.ProfileName @profile.ProfilePatronymic</th>
                        <th class="third-column">@profile.Phones.Where(ph => ph.PhonesIsPriority == true).FirstOrDefault()?.PhonesNum</th>
                        <th class="fourth-column">@profile.Mail.Where(ml => ml.MailsName == "potij_efozu23@yahoo.com").FirstOrDefault()?.MailsName</th>
                        <th class="fifth-column"></th>
                        <th class="sixth-column"></th>
                    </tr>
                }
            </table>
        </div>
    </div>
</body>
</html>